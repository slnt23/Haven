# AGENTS.md

本文件为 Codex 在 Haven 仓库中工作时提供约束。若本文与已接受的 ADR 冲突，以最新已接受 ADR 为准。

## 项目定位

**Haven**（Python 包名：`haven`）是面向患者的个人慢病管理智能体，当前聚焦高血压管理的 MVP 闭环。它不是通用多智能体框架、医疗诊断工具或在线问诊平台。

- 产品基线版本：`0.0.1`
- Python：`3.14+`
- 当前阶段：文档与项目骨架建设
- 权威设计文档：`.docs/adr/` 与 `.docs/requirements/`

## MVP 成功标准

```text
用户知情同意与建档
→ 手工录入血压
→ 数据校验与异常值二次确认
→ 查看七日趋势
→ 命中安全规则时给出保守的就医提示
```

用药提醒、医学知识问答、自然语言结构化提取、家庭协同和多智能体编排均不属于首个闭环，除非新的已接受 ADR 明确调整范围。

## 计划中的项目入口

当前入口尚未实现。实现顺序由 ADR-005 约束：

1. `haven web`：患者端 Web/API 主入口，首个产品入口。
2. `haven check`：配置、数据库和安全规则的只读自检入口。
3. 后续入口必须有明确用户需求和独立决策，不恢复旧项目的 CLI REPL、飞书或通用守护进程入口。

`pyproject.toml` 中不得声明指向不存在模块的命令入口。

## 目标代码边界

源码位于 `src/haven/`。业务执行采用单一 Agent；确定性校验、安全响应和趋势计算优先使用规则代码，不交给 LLM 自由推理。

| 目录 | 职责 |
| --- | --- |
| `config/` | 只通过类型化配置对象提供配置 |
| `domain/` | 健康画像、体征及安全事件等领域模型 |
| `application/` | 建档、记录体征、趋势查询等用例 |
| `infrastructure/` | 数据库、模型客户端、日志等适配器 |
| `safety/` | 确定性规则、安全状态机和模板 |
| `interface/` | Web/API 与命令入口 |

在首个用例出现前不要提前创建空层或通用注册框架。

## 常用命令

```bash
uv sync --group dev
uv run pytest
uv run ruff check .
uv run ruff format --check .
```

只有入口实现后，才把相应运行命令加入本文。

## 开发约定

- 配置优先级：内置默认值 < 工作目录配置 < 环境变量 < 运行时覆盖。
- 模块不得自行读取散落的 YAML 或环境变量，只依赖配置对象。
- 健康数据必须结构化存储；LLM 输出不得直接写入，需经过校验和用户确认。
- 医疗知识、风险阈值和安全规则必须记录来源、适用地区、版本及生效日期。
- 紧急识别是风险控制，不得宣称“100% 可靠”；关键路径必须覆盖误报、漏报和降级测试。
- 日志默认不记录健康数据、凭据或完整对话；审计数据遵循最小化原则。
- 只实现当前需求，不恢复旧多智能体框架的 Planner、Workflow、Skill 或多入口抽象。

## 修改准则

1. 实现前写明假设与可验证成功标准。
2. 用最少代码完成当前垂直切片，不为未来需求提前抽象。
3. 只修改与任务直接相关的代码和文档，保留用户已有改动。
4. 修复缺陷时先增加复现测试，再实现修复。
5. 新需求必须有稳定 ID，并能追踪到 ADR、验收标准和测试。
